<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lessons_schedule">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_calendar"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_calendar"
        android:supportsRtl="true"
        android:theme="@style/Theme.TimetableManager">
        <activity
            android:name="com.example.lessons_schedule.time_table.TimeTableActivity"
            android:exported="false">
            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity
            android:name="com.example.lessons_schedule.subjects.SubjectsActivity"
            android:exported="false">
            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity
            android:name="com.example.lessons_schedule.schedule.ScheduleActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>